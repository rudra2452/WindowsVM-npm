{
  "name": "simple-node-app",
  "version": "1.0.0",
  "description": "A minimal Express server for testing deployment on Azure Windows VM",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "azure",
    "vm",
    "demo"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

